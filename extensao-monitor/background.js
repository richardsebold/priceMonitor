// O fluxo de adição de produto agora acontece pelo popup (popup.html / popup.js).
// Este service worker existe apenas para satisfazer o manifest e pode ser
// usado futuramente para tarefas em background (ex.: badge, notificações).

self.addEventListener("install", () => {
});
